name: First Workflow

on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print 
        run: echo "Hello world"
      - name: Print 2
        run: echo "Hello w"
